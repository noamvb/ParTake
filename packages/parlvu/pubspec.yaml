name: parlvu
description: Unofficial client for ParlVU (House of Commons video) and openparliament.ca speech times.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.13.4

# Add regular dependencies here.
dependencies:
  http: ^1.6.0
  timezone: ^0.11.1
  # path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
